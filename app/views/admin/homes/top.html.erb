<div class='container py-5'>
  <div class='row justify-content-center'>
    <div class='col-md-10 col-lg-10'>
      <div class='row'>
        管理者トップページです。
      </div>

      <h4 class='my-3'>レビュー</h4>
      <table class='table table-hover my-5'>
        <tbody>
          <% @reviews.each do |review| %>
            <tr>
              <td>
                <div class="px-3 py-2">
                  <div class="d-flex justify-content-between align-items-center">
                    <h5>
                      <%= link_to review.title.presence || "タイトルなし", item_review_path(review.item, review), class: "font-weight-bold" %>
                    </h5>
                    <small>by <%= review.user.name %></small>
                  </div>

                  <div class="mb-2">
                    <%= render "public/shared/star", review: review %>
                  </div>

                  <div class="mb-2">
                    商品: <strong><%= review.item.name rescue review.item.title %></strong>
                  </div>
                  <div class="mb-2">
                    <i class="fas fa-tags text-primary"></i>
                    <% review.tag_list.each do |tag| %>
                      <span class="badge badge-info"><%= tag %></span>
                    <% end %>
                  </div>
                  <p class="mt-2 mb-0">
                    内容：<%= review.body rescue review.content %>
                  </p>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>